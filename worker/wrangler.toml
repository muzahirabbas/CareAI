name = "care-notifications-worker"
main = "index.ts"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Cron trigger for checking missed medications every hour
[triggers]
crons = ["* * * * *"]

[vars]
VAPID_PUBLIC_KEY = ""
VAPID_PRIVATE_KEY = ""
